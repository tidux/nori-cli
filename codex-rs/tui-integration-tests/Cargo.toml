[package]
name = "tui-integration-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
portable-pty = "0.8"
vt100 = "0.15"
insta = "1"
anyhow = "1"
tempfile = "3"

[target.'cfg(unix)'.dependencies]
nix = { version = "0.27", features = ["fs"] }
libc = "0.2"

[dev-dependencies]
tempfile = "3"
